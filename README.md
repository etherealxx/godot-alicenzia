#### Known Bugs (as of 0.0.24-alpha1)
- `res://` path is selectable (it should've not)
  - both adding a license data or removing data on `res://` path require refresh addon or selecting other file/folder on FileSystem dock in order to show said data on the table